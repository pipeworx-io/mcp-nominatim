{
  "name": "@pipeworx/mcp-nominatim",
  "version": "0.1.0",
  "description": "MCP server for geocoding and reverse geocoding via OpenStreetMap Nominatim API",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src"],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "keywords": ["mcp", "mcp-server", "model-context-protocol", "pipeworx", "nominatim", "geocoding", "openstreetmap"],
  "author": "Pipeworx",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
